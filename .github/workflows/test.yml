name: Test Ollama Model Workflow

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Create .env file
      run: make setup

    - name: Start Ollama service
      run: make up

    - name: Wait for Ollama to be ready
      run: |
        echo "Waiting for Ollama service to be ready..."
        timeout 60 bash -c 'until curl -f http://localhost:11434/api/tags 2>/dev/null; do sleep 2; done'
        echo "Ollama is ready!"

    - name: Pull base model
      run: |
        echo "Pulling llama3.2:1b base model (1.3GB, lightweight for testing)..."
        docker compose exec ollama ollama pull llama3.2:1b
        echo "Base model ready!"

    - name: Run quick test
      run: |
        echo "Running end-to-end test..."
        echo "This will create a test model, send a prompt, verify response, and cleanup."
        echo "y" | make quick-test
        echo ""
        echo "Test completed! Check output above for model response."

    - name: Test full model export/import roundtrip
      run: |
        echo "Exporting llama3.2:1b with weights..."
        bash scripts/export-model-full.sh llama3.2:1b ./backups/full
        ARCHIVE=$(ls ./backups/full/llama3.2-1b-*.tar)
        echo "Removing the model to prove the archive restores it..."
        docker compose exec ollama ollama rm llama3.2:1b
        echo "Importing archive..."
        bash scripts/import-model-full.sh "$ARCHIVE"
        echo "Verifying the restored model responds..."
        docker compose exec -T ollama ollama run llama3.2:1b "Reply with the single word: OK"
        rm -rf ./backups/full

    - name: List models (for debugging)
      if: always()
      run: make list-models

    - name: Show logs on failure
      if: failure()
      run: make logs

    - name: Cleanup
      if: always()
      run: make down
